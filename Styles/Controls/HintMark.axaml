<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Файл оставлен пустым намеренно.

	     Оформление HintMark задано в самом контроле
	     (Src/Infrastructure/Controls/HintMark.cs): контрол компилируется
	     в две сборки — приложение и общие контролы, — а стиль отсюда
	     применился бы только к типу из приложения. Селектор сверяет тип
	     вместе с его сборкой, поэтому значок в палитрах остался бы
	     без шаблона.

	     Файл можно удалить из проекта; он не подключён в Controls.axaml. -->
</Styles>
