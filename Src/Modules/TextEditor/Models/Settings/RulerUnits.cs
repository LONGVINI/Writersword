namespace Writersword.Modules.TextEditor.Models.Settings
{
    /// <summary>
    /// Единицы измерения линейки.
    /// Переключаются в настройках редактора.
    /// </summary>
    public enum RulerUnits
    {
        /// <summary>Сантиметры — используются по умолчанию в большинстве стран.</summary>
        Centimeters = 0,

        /// <summary>Дюймы — используются в США и ряде других стран.</summary>
        Inches = 1
    }
}